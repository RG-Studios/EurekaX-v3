from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parent
SRC = ROOT / "src"
if str(SRC) not in sys.path:
    sys.path.insert(0, str(SRC))

from eurekax.memory import DEFAULT_MEMORY_FILE as MEMORY_FILE, store_insight
